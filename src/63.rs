fn main() {
    // Define variables
    let x = 5;
    let y = 3;

    // Perform arithmetic operations
    let result = (x + y) * 4;

    // Print the result
    println!("The result is: {}", result);
}
